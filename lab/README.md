The `lab` CLI is documented in the [repository README](../README.md): the workflow, the guarantees, the vLLM
engine and its rollback, and the two benchmark methods.

```sh
cd ~/llm-lab/lab
uv run lab --help
uv run lab doctor
uv run pytest
```

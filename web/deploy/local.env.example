# Copy to web/deploy/local.env (gitignored) on the GPU host; push.sh reads it.
# How to reach the web host: any ssh-like command that runs its last argument there.
WEB_SSH=tailscale ssh you@web
# The app directory on the web host: it holds .env, repo/, repo.prev/ and logs/.
WEB_APP_DIR=/opt/llmlab
